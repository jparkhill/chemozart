var ob = require('./../../index');

var conversion = new ob.Conversion();
var builder = new ob.Builder();

console.log('Calculation Energy for Cyclopentane');

var mol = conversion
    .setInFormat('smiles')
    .read('C1CCCC1');

builder.build(mol);

console.log(mol.toJSON());
